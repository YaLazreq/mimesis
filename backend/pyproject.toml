[project]
name = "mimesis"
version = "0.1.0"
dependencies = [
    "fastapi",
    "uvicorn",
    "websockets",
    "google-adk",
    "python-dotenv",
    "mcp",
    "httpx",
]

[tool.ruff]
line-length = 80